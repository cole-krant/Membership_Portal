<%- include ('../partials/header') %>
<%- include ('../partials/menu') %>

<main>
  <header>
    <style>
      .maroon_bells_bg {
        background-image: url('../../resources/img/Maroon_Bells.jpg');
        background-repeat: no-repeat;
        background-attachment: fixed;
        background-size: cover
      }
    </style>
  </header>
    <body>
      <!--  MEET THE TEAM CONTAINER  -->
      <div class="maroon_bells_bg">
              
        <div class="w3-container w3-padding-64 w3-center" id="team">
            <h1> OUR TEAM </h1>
            <h5 style="color: white;"> Meet the Bridge Education Committee </p>
    
            <!--  BRIDGE COMMITEE  -->
            <div class="w3-row"><br>

                <div class="w3-quarter">
                    <img src="../../resources/pfp/Default_Avatar.jpg" alt="Boss" style="width:45%" class="w3-circle w3-hover-opacity">
                    <h3 style="color: white;">Katie McDonald</h3>
                    <h5 style="color: white;">Pledge Educator</p>
                </div>
                
                <div class="w3-quarter">
                    <img src="../../resources/pfp/Default_Avatar.jpg" alt="Boss" style="width:45%" class="w3-circle w3-hover-opacity">
                    <h3 style="color: white;">TBD</h3>
                    <h5 style="color: white;">Committee Coordinator</p>
                </div>
                
                <div class="w3-quarter">
                    <img src="../../resources/pfp/Default_Avatar.jpg" alt="Boss" style="width:45%" class="w3-circle w3-hover-opacity">
                    <h3 style="color: white;">TBD</h3>
                    <h5 style="color: white;">Networking Director</p>
                </div>

                <div class="w3-quarter">
                    <img src="../../resources/pfp/Default_Avatar.jpg" alt="Boss" style="width:45%" class="w3-circle w3-hover-opacity">
                    <h3 style="color: white;">TBD</h3>
                    <h5 style="color: white;">Professional Development Coordinator</p>
                </div>

            </div>

            <div class="row">
              <div class="w3-quarter">
                  <img src="../../resources/pfp/Cole_Krant.jpg" alt="Boss" style="width:45%" class="w3-circle w3-hover-opacity">
                  <h3>Cole Krant</h3>
                  <h5>VP of Membership</p>
              </div>
          </div>

        </div>
    </div>


    <!-- MyBrotherhood -->
    <div class="container text">
      <h1>MyBrotherhood</h1>
  
      <div class="card-group text-black">
        
        <style>
            /* COMMUNITY CARD CSS */
            .card {
              box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
              max-width: 300px;
              margin: auto;
              text-align: center;
              font-family: arial;
            }
            
            .title {
              color: white;
              font-size: 18px;
            }
            
            button {
              border: none;
              outline: 0;
              display: inline-block;
              padding: 8px;
              color: white;
              background-color: #000;
              text-align: center;
              cursor: pointer;
              width: 100%;
              font-size: 18px;
            }
            
            a {
              text-decoration: none;
              font-size: 22px;
              color: black;
            }
  
            img {
              width:100%;
              max-width:424px;
            }
            
            button:hover, a:hover {
              opacity: 0.7;
            }
        </style>
        <%
            var count_col = 0;      // variable to limit 4 cards per row
            var start = 0;          // variable to limit 4 cards per row
            var count_cmp = 4;      // variable to track how many cards have been input per row
            var pseudo = '';        // variable to code html
            pseudo = pseudo + '<div class="container-fluid">';
  
        community.forEach(community => {
            if(community) {
                if(count_col === start) {
  
                    count_col = count_col + 1;                                  // incriment count
                    pseudo = pseudo + '<div class="card-group text-black">';    // begin the row row
                    pseudo = pseudo + '<div class="card">';                     // create a card
                    pseudo = pseudo + '<img width="100px" max-width="50px" src="' + community.img + '" alt="Profile Picture">';
                    pseudo = pseudo + '<h1>' + community.username + '</h1>';
                    pseudo = pseudo + '<p class="title">' + community.major + '</p>';
                    pseudo = pseudo + '</div>';
                    pseudo = pseudo + '<br><br>';
  
                }
                else if(count_col < count_cmp) {
  
                    count_col = count_col + 1;                                  // incriment count
                    pseudo = pseudo + '<div class="card">';                     // same row, input card
                    pseudo = pseudo + '<img width="100px" max-width="50px" src="' + community.img + '" alt="Profile Picture" style="width:100%">';
                    pseudo = pseudo + '<h1>' + community.username + '</h1>';
                    pseudo = pseudo + '<p class="title">' + community.major + '</p>';
                    pseudo = pseudo + '</div>';
                    pseudo = pseudo + '<br><br>';
  
                }
                else if(count_col === count_cmp) {
  
                    pseudo = pseudo + '</div>';         // close the card
                    pseudo = pseudo + '<br><br>';
                    start = count_col;                  // indicate new row
                    count_cmp = count_cmp + 4;          // set new ceiling
  
                }
            }
          }) 
          pseudo = pseudo + '</div>';         // close the card group
          %>
        <%- pseudo %>
      </div>
    </div>

  </body>
</main>

<%- include ('../partials/footer') %>